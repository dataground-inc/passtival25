# PRD - 전국 실기 연합 테스트 실기 기록 및 실시간 순위 확인 시스템 개선

## 1. 개요

전국 실기 연합 테스트 참가 학생이 수험번호를 입력하여 본인의 실기 기록과 현재 순위를 확인하고, 그룹별 TOP 5 순위를 조회할 수 있도록 기존 시스템을 개선한다.

현재 프로젝트의 기술 구조와 데이터 연동 방식은 최대한 유지하며, 화면 구성과 사용자 흐름을 중심으로 개선한다.

디자인은 제공된 Figma 디자인의 분위기와 레이아웃을 기준으로 적용하되, 사용성과 완성도를 높일 수 있는 범위에서 보다 프리미엄한 형태로 고도화할 수 있다.

---

## 2. 개발 목적

- 학생이 수험번호만으로 본인의 실기 기록과 순위를 간편하게 확인할 수 있도록 개선
- 개인 기록과 순위 정보를 한 화면에서 직관적으로 확인할 수 있도록 구성
- 참가 그룹별 상위 참가자를 확인할 수 있는 TOP 5 순위 기능 제공
- 행사 종료 후에도 일정 기간 결과를 조회할 수 있는 형태로 구축
- 기존 코드와 데이터 구조를 최대한 활용하여 개발 범위 최소화

---

## 3. 기존 시스템 구조

현재 시스템은 다음 구조를 사용한다.

Google Spreadsheet
→ Google Apps Script API
→ 프런트엔드 화면

### 주요 기능

- 수험번호를 기준으로 특정 학생 정보 및 실기 기록 조회
- 참가 그룹을 기준으로 총점 상위 5명 조회

기존 기술 구조는 유지하되, 개선 화면에서 필요한 데이터를 정상적으로 반환할 수 있도록 Apps Script의 일부 데이터 매핑을 수정한다.

개발 착수 전 현재 프로젝트의 페이지, 컴포넌트 및 데이터 연동 구조를 먼저 확인하고, 기존 구조를 최대한 활용하여 작업한다.

---

## 4. 개발 범위

### 포함 범위

- 시작 화면 개선
- 수험번호 입력 바텀 시트 추가
- 개인 실기 기록 확인 화면 개선
- 개인 조회 결과에 성별, 학년, 구분 데이터 추가
- 기존 20m 종목을 좌전굴로 변경
- 미응시 기록 표시
- 그룹별 TOP 5 순위 페이지 신규 구성
- 새로고침 시 최신 데이터 조회
- 모바일 중심 반응형 화면 구성

### 제외 범위

- 별도의 회원가입 및 로그인 기능
- 수험번호 외 추가 본인 인증
- 스프레드시트 기반 운영 구조 변경
- 순위 자동 갱신
- 개인 화면 내 총점 노출
- 전체 참가자 순위 공개
- 관리자용 기록 입력 화면 개선

---

## 5. 사용자 플로우

### 5.1 개인 기록 조회

1. 사용자가 시작 화면에 접속
2. 시작하기 버튼 클릭
3. 화면 하단에 수험번호 입력 바텀 시트 노출
4. 수험번호 입력
5. 해당 수험번호의 학생 데이터 조회
6. 개인 실기 기록 확인 페이지로 이동
7. 학생 정보, 현재 순위 및 실기 기록 확인

### 5.2 TOP 5 순위 조회

1. 사용자가 TOP 5 순위 확인 기능 선택
2. 순위 확인 페이지로 이동
3. 상단 탭에서 조회할 참가 그룹 선택
4. 선택한 그룹의 TOP 1~5 확인
5. 페이지를 새로고침하면 최신 스프레드시트 데이터 조회

TOP 5 순위 페이지로 진입하는 버튼의 위치와 구체적인 이동 방식은 개발 과정에서 추가 논의하여 결정한다.

---

## 6. 화면 구성

### 6.1 시작 화면

#### 목적

서비스의 첫 진입 화면으로, 사용자가 실기 기록 조회를 시작할 수 있도록 한다.

#### 주요 구성

- 행사 또는 서비스 브랜드 이미지
- 메인 슬로건
- 시작하기 버튼

#### 주요 기능

- 시작하기 버튼 클릭 시 수험번호 입력 바텀 시트 노출
- 제공된 Figma 디자인의 블랙·블루 기반 분위기 유지
- 모바일 화면을 우선 기준으로 구현

---

### 6.2 수험번호 입력 바텀 시트

#### 목적

별도의 입력 페이지로 이동하지 않고 간단하게 수험번호를 입력하여 개인 기록을 조회할 수 있도록 한다.

#### 주요 구성

- 안내 문구
- 수험번호 입력 필드
- 기록 확인하기 버튼
- 바텀 시트 닫기 기능

#### 주요 기능

- 수험번호 입력 후 학생 데이터 조회
- 정상적으로 조회되면 개인 실기 기록 확인 페이지로 이동
- 조회되지 않는 수험번호는 오류 문구 노출
- 추가 본인 인증은 적용하지 않음

#### 안내 문구 예시

수험번호를 입력하고 나의 실기 기록을 확인해 주세요.

#### 오류 문구 예시

수험번호를 확인할 수 없습니다. 다시 입력해 주세요.

---

### 6.3 개인 실기 기록 확인 화면

#### 목적

조회한 학생의 기본 정보, 소속 그룹 내 현재 순위 및 종목별 실기 기록을 보여준다.

#### 학생 정보

- 이름
- 수험번호
- 소속
- 학년
- 성별
- 구분

구분은 다음 4개 값 중 하나로 노출한다.

- 고3 이상 남자
- 고3 이상 여자
- 고2 이하 남자
- 고2 이하 여자

#### 순위 정보

- 해당 학생의 구분 내 현재 순위
- 해당 학생과 같은 그룹의 전체 참가자 수

순위는 스프레드시트에 저장된 그룹별 순위 값을 그대로 사용한다.

#### 노출 예시

현재 순위
233위

총 1,233명 중

#### 실기 기록 정보

- 제자리멀리뛰기
- 배근력
- 10m 왕복달리기
- 메디신볼던지기
- 좌전굴

기록값이 없거나 미입력된 종목은 미응시로 표시한다.

총점 데이터는 순위 산정과 TOP 5 추출에 사용하지만 개인 실기 기록 화면에는 노출하지 않는다.

#### 안내 문구

실기 기록이 잘못되었다면 근처 기록 작성 스태프에게 문의해 주세요.

별도의 문의 버튼이나 연락처는 제공하지 않는다.

---

### 6.4 TOP 5 순위 확인 화면

#### 목적

참가 그룹별 총점 기준 상위 5명의 이름과 소속을 확인할 수 있도록 한다.

#### 그룹 탭

화면 상단에 다음 4개 그룹 탭을 배치한다.

- 고3 이상 남자
- 고3 이상 여자
- 고2 이하 남자
- 고2 이하 여자

사용자가 탭을 선택하면 해당 그룹의 TOP 5 데이터만 노출한다.

#### 노출 정보

각 순위별로 다음 정보만 표시한다.

- 순위
- 이름
- 소속

학생 이름은 마스킹하지 않고 전체 이름을 노출한다.

총점과 종목별 실기 기록은 TOP 5 순위 화면에 노출하지 않는다.

#### 데이터 갱신

- 페이지 진입 시 최신 데이터 조회
- 사용자가 페이지를 새로고침하면 최신 데이터 재조회
- 별도의 자동 갱신 기능은 적용하지 않음

#### 디자인 방향

TOP 5 순위 화면은 별도의 Figma 디자인이 없는 상태이므로 기존 화면의 브랜드 분위기를 기준으로 신규 구성한다.

- 블랙·네이비·블루 계열의 배경
- 스포츠 대회에 적합한 강한 타이포그래피
- 1위부터 5위까지 순위가 직관적으로 보이는 구조
- 1~3위는 상대적으로 강조
- 기존 개인 기록 화면과 시각적 일관성 유지

---

## 7. 데이터 구성

### 7.1 학생 정보

- 수험번호
- 이름
- 성별
- 학년
- 소속
- 구분

### 7.2 실기 기록 정보

- 제자리멀리뛰기 기록
- 배근력 기록
- 10m 왕복달리기 기록
- 메디신볼던지기 기록
- 좌전굴 기록
- 총점
- 순위

### 7.3 데이터 활용 기준

- 총점: 그룹별 TOP 5 정렬에만 사용
- 순위: 개인 실기 기록 화면에 노출
- 구분: 개인 순위 그룹 및 TOP 5 순위 탭 분류에 사용
- 실기 기록: 개인 실기 기록 화면에 노출

---

## 8. Apps Script 수정 방향

기존 Apps Script의 API 구조와 조회 방식을 유지하되, 개인 조회 결과의 데이터 매핑과 반환 항목을 수정한다.

### 8.1 개인 조회 결과

개인 조회 결과에 다음 데이터를 추가한다.

- 성별
- 학년
- 구분
- 소속

기존 `run20m` 필드는 실제 데이터에 맞게 좌전굴을 의미하는 필드명으로 변경한다.

```javascript
const result = {
  examNumber: match[0],
  name: match[1],
  gender: match[2],
  grade: match[3],
  center: match[4],
  jemul: match[5],
  backStrength: match[6],
  run10m: match[7],
  medicineBall: match[8],
  sitAndReach: match[9],
  rank: match[11],
  group: match[12],
};
```

실제 구분 데이터의 열 위치는 현재 스프레드시트 구조를 확인하여 적용한다.

기존 코드의 `center` 데이터 매핑도 실제 소속 데이터가 반환되도록 수정한다.

```javascript
// AS-IS
center: match[0];

// TO-BE
center: match[4];
```

### 8.2 TOP 5 조회 결과

TOP 5는 기존과 동일하게 그룹별 참가자를 총점 기준 내림차순으로 정렬하여 상위 5명을 추출한다.

화면에서는 다음 데이터만 사용한다.

```javascript
{
  name: row[1],
  center: row[4]
}
```

총점은 정렬을 위한 내부 데이터로만 사용하며 사용자 화면에는 노출하지 않는다.

현재 스프레드시트에 구분 데이터가 별도로 존재하므로, 가능하다면 학년과 성별을 다시 조합하기보다 해당 구분값을 기준으로 필터링하는 방식도 검토한다.

---

## 9. 디자인 요구사항

- 제공된 Figma 디자인을 주요 레퍼런스로 활용
- 현재 시작 화면과 개인 실기 기록 화면의 분위기 유지
- 기존 레이아웃을 단순 복제하기보다 정보 가독성과 완성도를 개선할 수 있음
- 모바일 퍼스트 방식으로 개발
- 태블릿과 PC에서도 정상적으로 이용할 수 있도록 반응형 적용
- 이름, 순위, 기록값 등 주요 정보가 우선적으로 인지되도록 구성
- 수험번호 입력은 별도 페이지가 아닌 바텀 시트로 구현
- TOP 5 순위 화면은 기존 브랜드 톤에 맞춰 신규 디자인

---

## 10. 주요 상태 처리

### 수험번호 조회 실패

입력한 수험번호와 일치하는 학생이 없는 경우 오류 문구를 노출하고 다시 입력할 수 있도록 한다.

### 실기 미응시

기록값이 없거나 미입력된 종목은 미응시로 표시한다.

### 데이터 조회 실패

스프레드시트 또는 Apps Script 데이터를 불러오지 못한 경우 다시 시도할 수 있는 안내를 노출한다.

### TOP 5 인원 부족

특정 그룹의 참가자가 5명 미만인 경우 조회 가능한 인원까지만 노출한다.

---

## 11. 추가 논의 필요 사항

다음 항목은 화면 개발 및 브레인스토밍 과정에서 최종 결정한다.

- TOP 5 순위 화면 진입 버튼의 위치
- 시작 화면에서 TOP 5 순위로 바로 진입할 수 있는지 여부
- 개인 실기 기록 화면 하단 버튼 구성
- 개인 실기 기록 화면과 TOP 5 순위 화면 간 이동 방식
- TOP 1~3의 시각적 강조 방식
- 서비스 조회 가능 기간
- 조회 기간 종료 이후 화면 안내

---

## 12. 개발 완료 기준

- 시작 화면에서 수험번호 입력 바텀 시트를 열 수 있음
- 수험번호 조회 후 해당 학생의 개인 기록 화면으로 이동함
- 이름, 수험번호, 소속, 학년, 성별, 구분이 정상적으로 노출됨
- 해당 학생이 속한 그룹의 순위와 참가자 수가 노출됨
- 5개 실기 종목 기록이 정상적으로 노출됨
- 기존 20m 관련 데이터가 좌전굴로 변경됨
- 기록이 없는 종목은 미응시로 표시됨
- TOP 5 순위 화면에서 4개 그룹 탭을 전환할 수 있음
- 각 그룹의 TOP 1~5 이름과 소속이 정상적으로 노출됨
- 페이지 새로고침 시 최신 스프레드시트 데이터가 반영됨
- 제공된 Figma 디자인과 일관된 모바일 중심 UI로 구현됨
